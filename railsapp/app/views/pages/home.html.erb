  <div class=" mx-auto max-w-4xl my-12 space-y-6 rounded flex justify-between items-center ">
    <div class="text-center bg-white rounded-lg shadow-lg p-6 ml-8">
      <p class="text-xl font-semibold text-gray-700">Statistics:</p>
      <p class="text-gray-700">Total Customers: <%= @customers.count %></p>
      <p class="text-gray-700">High churn chance: <%= @customers.select { |customer| customer.risco_de_churn >= 0.8 }.count %></p>
      <p class="text-gray-700">Invalid cards: <%= @cards.select { |card| !card.card_status }.count %></p>
    </div>
    <div class="w-full max-w-lg p-8">
      <div class="bg-white rounded-lg shadow-lg p-6">
        <h2 class="text-lg font-medium mb-6">Payment Information</h2>
        <%= form_with(url: "/cartao", method: "post", local: false) do |form| %>
  <div class="grid grid-cols-2 gap-6">
    <div class="col-span-2 sm:col-span-1">
      <%= form.label :card_number, class: "block text-sm font-medium text-gray-700 mb-2" do %>
            Card Number
          <% end %>
          <%= form.text_field :card_number, placeholder: "0000 0000 0000 0000", class: "w-full py-3 px-4 border border-gray-400 rounded-lg focus:outline-none focus:border-blue-500" %>
        </div>
        <div class="col-span-2 sm:col-span-1">
          <%= form.label :expiration_date, class: "block text-sm font-medium text-gray-700 mb-2" do %>
            Expiration Date
          <% end %>
          <%= form.text_field :expiration_date, placeholder: "MM / YY", class: "w-full py-3 px-4 border border-gray-400 rounded-lg focus:outline-none focus:border-blue-500" %>
        </div>
        <div class="col-span-2 sm:col-span-1">
          <%= form.label :cvv, class: "block text-sm font-medium text-gray-700 mb-2" do %>
            CVV
          <% end %>
          <%= form.text_field :cvv, placeholder: "000", class: "w-full py-3 px-4 border border-gray-400 rounded-lg focus:outline-none focus:border-blue-500" %>
        </div>
        <div class="col-span-2 sm:col-span-1">
          <%= form.label :card_holder, class: "block text-sm font-medium text-gray-700 mb-2" do %>
            Card Holder
          <% end %>
          <%= form.text_field :card_holder, placeholder: "Full Name", class: "w-full py-3 px-4 border border-gray-400 rounded-lg focus:outline-none focus:border-blue-500" %>
        </div>
      </div>
      <div class="mt-8">
        <%= form.submit "Submit", class: "w-full bg-green-500 hover:bg-blue-600 text-white font-medium py-3 rounded-lg focus:outline-none" %>
      </div>
    <% end %>
      </div>
    </div>
  </div>
<div data-controller="filter" class="mx-auto max-w-4xl my-12 space-y-6">
  <!-- Statistics Div -->

  <!-- Filtering and Search Box -->
  <div class="bg-white p-4 rounded-md space-x-4 flex items-center justify-between">
    <label class="text-sm font-medium text-gray-700">Risco de Churn:</label>
    <select data-filter-target="riscoDeChurn" data-action="change->filter#filter" class="border p-2" id="filterRiscoDeChurn">
      <option value="asc">Crescente</option>
      <option value="desc">Decrescente</option>
    </select>


    <label class="text-sm font-medium text-gray-700">Filtrar por Status do Cartão:</label>
    <select data-filter-target="cardStatus" data-action="change->filter#filter" class="border p-2" id="filterCardStatus">
      <option value="">All</option>
      <option value="valid">Valid</option>
      <option value="invalid">Invalid</option>
    </select>
  </div>
<!-- Card List Table -->
  <div data-filter-target="list">
    <%= render "card_list" %>
  </div>

</div>
